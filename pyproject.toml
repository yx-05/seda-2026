[project]
name = "seda-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "tqdm>=4.68.3",
]

[tool.uv.workspace]
members = [
    "data_analysis",
]
